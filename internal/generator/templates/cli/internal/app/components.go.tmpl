// Package app component registration
package app

// RegisterComponents registers components
//
// Note: Config and Logger are auto-registered by framework
func (c *{{.AppNamePascal}}App) RegisterComponents() {
	// Add components when needed:
	// c.Core.Register(
	// 	database.NewComponent(),
	// 	redis.NewComponent(),
	// )
}
